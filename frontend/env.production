# Production Environment Variables
# This file should be used for production deployment

# API Configuration - Use relative URLs in production to avoid mixed content
# Vercel will proxy /api requests to your HTTP backend
VITE_PREFIX_URL=/api

# Alternative: If you deploy backend to a service with HTTPS, update this URL
# VITE_PREFIX_URL=https://your-backend-domain.com/api
